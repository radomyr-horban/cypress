{
  "name": "task-2-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:run": "npx cypress run",
    "cypress:open": "npx cypress open",
    "cypress:run:headed": "npx cypress run --headed",
    "cypress:run:browser": "npx cypress run --browser",
    "cypress:dashboard": "npx cypress run --record"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.3.0"
  }
}
